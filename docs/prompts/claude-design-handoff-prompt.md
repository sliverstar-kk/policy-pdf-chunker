Use Superpowers to brainstorm and write a design spec for this repository.

Requirements:

- Read `CLAUDE.md` before proposing the workflow.
- Save the approved spec under `docs/superpowers/specs/`.
- Save the implementation plan under `docs/superpowers/plans/`.
- Plan explicitly for strict TDD.
- Break implementation into small slices.
- For every slice, identify the test file to create or update first.
- Stop after the plan is complete.
- Do not implement production code.

End with this exact handoff sentence:

Design and plan are approved for Codex implementation. Codex should now execute in strict RED-GREEN-REFACTOR order and should not change the design without surfacing a plan or spec amendment.

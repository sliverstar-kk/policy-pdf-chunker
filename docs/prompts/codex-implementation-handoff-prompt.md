Read `docs/ai-collaboration-playbook.md`, then read the latest approved spec and plan under `docs/superpowers/`.

Implementation requirements:

- Implement only the next planned slice.
- Follow strict RED-GREEN-REFACTOR.
- Do not write production code before a failing test.
- Keep changes scoped to the approved design.
- If the spec or plan is incomplete, stop and request an amendment instead of guessing.

When you finish, summarize:

- what was implemented
- which tests were added or updated
- any design ambiguity that should go back to Claude
